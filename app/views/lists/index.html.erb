<div class="container">
  <h1>Lists</h1>
  <ul>
    <% @lists.each do |list| %>
      <li><%= list.name %> <%= link_to 'Show', list %></li>
    <% end %>
  </ul>
  <%= link_to 'New List', new_list_path %>
</div>
